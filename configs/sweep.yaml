project: "sweep"
program: "training.py"
name: "hyperparams3"
method: grid
metric:
  goal: minimize
  name: Test_Backdoor_Accuracy_Top-1
parameters:
  batch_size:
    values: [64, 256, 512]
  lr:
    values: [0.1, 0.01, 0.001]
  optimizer:
    values: ['Adam', 'SGD']
  decay:
    values: [0.0005, 0.00005, 0.005]
  momentum:
    values: [0.8, 0.9, 0.95]
  random_seed:
    values: [1, 2, 3, 4, 5]
  poisoning_proportion:
    values: [0.001, 0.005, 0.01]
command:
  - ${env}
  - python
  - ${program}
  - "--params"
  - "configs/mnist_params.yaml"